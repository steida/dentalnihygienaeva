{
  "name": "dentalnihygienaeva",
  "private": true,
  "license": "MIT",
  "engines": {
    "node": "10.x.x"
  },
  "scripts": {
    "dev": "next",
    "build": "next build",
    "start": "next start",
    "deps": "rm -rf package-lock.json node_modules && npm i",
    "export": "npm run build && next export",
    "deploy": "run-s export && cd out && now && cd .. && rm -rf out"
  },
  "dependencies": {
    "next": "^7.0.2",
    "react": "16.7.0-alpha.2",
    "react-dom": "16.7.0-alpha.2"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
